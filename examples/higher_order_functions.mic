func map(a, f)
    i = 0
    while i < len(a)
        a:i = f(a:i)
    end
    return a
end

func filter(a, f)
    i = 0
    while i < len(a)
        if !f(a:i)
            del(a, i)
        end
    end
    return a
end